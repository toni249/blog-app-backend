REST ARCHITECTURE:

representational(format like json,yaml,xml)  state(data) transfer(carry data using http protocol)

constraint:
    client server architecture
    stateless
    cacheable
    uniform interface
    layered system
    code on demand

rest resources: anything u want to expose to outer world

URI: uniform resource identifier

rest vs soap

Http methods: GET, POST, PUT, DELETE, PATCH

